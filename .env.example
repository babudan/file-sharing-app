# Copy to .env before `docker compose up`.
# These values match docker-compose.yml defaults.

JWT_SECRET=caf22c37324de30a74afde154139e894d2e51038ba711f1e5d5d9fe714c993b12309c60fdab9b2a3eab559176a07fac622a9117d
9bbd31a177f02c73d7b7910d
COOKIE_SECURE=false

# Used only for local (non-Docker) development.
# Inside Docker these are overridden by compose.
DATABASE_URL=postgresql://fileshare:fileshare@localhost:5432/fileshare
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=fileshare
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_FORCE_PATH_STYLE=true

CORS_ORIGIN=http://localhost:5173
PUBLIC_APP_URL=http://localhost:8080
PORT=3000
NODE_ENV=development
MAX_FILE_BYTES=26214400

# Optional: workspace invites are emailed when both are set.
# Verify SENDGRID_FROM_EMAIL in SendGrid (Sender Authentication) first.
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=babudan517@gmail.com
SENDGRID_FROM_NAME=arindam-dan

